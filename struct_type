#include <stdio.h>

typedef struct grades {
  char names[20];
  int kor;
  int eng;
  int mat;
} GRADE_T;

main ()
{
  GRADE_T student[2];
  
  strcpy(student[0].name, "Peter");
  student[0].kor = 81;
  student[0].eng = 73;
  student[0].mat = 93;
  
  printf("Name  : %s\n", student[0].name);
}
