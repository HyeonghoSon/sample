#include <stdio.h>

typedef struct Grades {
  char names[20];
  int kor;
  int eng;
  int mat;
};

main ()
{
  Grades student[2];
  
  strcpy(student[0].name, "Peter");
  student[0].kor = 81;
  student[0].eng = 73;
  student[0].mat = 93;
}
